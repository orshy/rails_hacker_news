<%= render 'layouts/errors', :object => @link %>

<%= form_for @link do |f| %>
  <%= f.label :name  %>
  <%= f.text_field :name, :placeholder => "here" %>
  <%= f.label :url %>
  <%= f.text_field :url, :placeholder => "here" %>
  <%= f.button :submit, class: "btn btn-sm btn-success"  %>
<% end %>
